function y = my_func_2(x)
    % 一个测试函数，分段函数，不连续
    % 输入:要求的变量，可以为标量、向量或者矩阵
    % 输出:与x的size相同的量，为计算的函数值
    id1 = ((x >= -1) & (x < 0));
    id2 = ((x >= 0) & (x < 0.5));
    id3 = ((x >= 0.5) & (x <= 1));
    y = sin(pi * x) .* id1 + cos(pi * x) .* id2;
end
